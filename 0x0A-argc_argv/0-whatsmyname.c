#include<stdio.h>
#include "main.h"

/**
 * main - prints prog name
 * @argc: argv count
 * @argv: array of args
 *
 * Return:0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf(argv[0]);
return (0);
}
